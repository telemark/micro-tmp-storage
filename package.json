{
  "name": "micro-tmp-storage",
  "description": "Temporary storage",
  "version": "1.0.5",
  "license": "MIT",
  "private": true,
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "main": "index.js",
  "engines": {
    "node": ">=7.5.0"
  },
  "scripts": {
    "test": "standard && nsp check && tap --reporter=spec test/**/*.js",
    "test-offline": "standard && tap --reporter=spec test/**/*.js",
    "coverage": "tap test/**/*.js --coverage",
    "coveralls": "tap --cov --coverage-report=lcov test/**/*.js && cat coverage/lcov.info | coveralls",
    "setup": "npm install",
    "standard-fix": "standard --fix",
    "start": "micro -p 8080",
    "dev": "micro",
    "now-deploy": "npm test && now --npm --alias"
  },
  "keywords": [
    "microservice",
    "micro",
    "storage",
    "temporary",
    "Telemark fylkeskommune"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/telemark/micro-tmp-storage.git"
  },
  "bugs": {
    "url": "https://github.com/telemark/micro-tmp-storage/issues"
  },
  "now": {
    "aliases": [
      "tmp.storage.micro.t-fk.no"
    ],
    "env": {
      "NODE_ENV": "production"
    }
  },
  "devDependencies": {
    "coveralls": "2.11.16",
    "nsp": "2.6.2",
    "standard": "8.6.0",
    "tap": "10.2.1"
  },
  "dependencies": {
    "marked": "0.3.6",
    "micro": "7.0.6",
    "node-cache": "4.1.1",
    "uuid": "3.0.1"
  }
}
